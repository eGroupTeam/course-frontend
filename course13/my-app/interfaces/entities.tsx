export type Product = {
  id: number, name:string, price:number
}
export type Login = {
  id:string, password:string
}

export type Customer = {
  id: number, address: string, name: string, weight:number
}