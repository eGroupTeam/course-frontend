export type Product = {
    id: number, name: string, desc: string, order: number, price: number, dep: number
}

export type Organization = {
    id: number, name: string, set_time: string, desc: string, contactnum: number, mail: string, address: string
}
