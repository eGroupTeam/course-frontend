export type Product = {
    id: number, name: string, expla: string, seq: number, price: number, org_id: number
}

export type Organization = {
    id: number, date: string, name: string, intro: string, phone: string, email: string, address: string
}