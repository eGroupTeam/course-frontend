import React, { useState } from 'react';

function App() {
  const [count, setCount] = useState(0);

  const handleClick = () => {
    setCount(count + 1);
  };

  return (
    <div>
      <p>數字：{count}</p>
      <button onClick={handleClick}>加1</button>
    </div>
  );
}

export default App;